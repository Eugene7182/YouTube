name: Ping Render queue
on:
  schedule:
    - cron: '0 17 * * 1,3,5'  # Mon/Wed/Fri 17:00 UTC
  workflow_dispatch: {}
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl queue endpoint
        run: |
          echo "Set your Render URL here"; exit 0
